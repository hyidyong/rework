# 사람이 직접 확인할 항목

시스템은 로컬 Supabase 키 감지, DB 마이그레이션, 데모 파이프라인, Git 커밋·푸시까지 자동화되어 있습니다. 아래 항목은 계정·비용·학술 책임 때문에 사람의 결정이 필요한 선택 사항입니다.

1. PC 재부팅 후 Docker Desktop이 실행 중인지 확인합니다. 실행되지 않았다면 Docker Desktop을 한 번 연 뒤 `npm run supabase:start`를 실행합니다.
2. 실제 OpenAI 모델을 쓸 때만 Git에서 제외된 `.env`에 본인의 `OPENAI_API_KEY`를 넣습니다. 키가 없으면 전체 시스템은 데모 모델로 정상 작동합니다. 키를 채팅, 이슈, 커밋에 붙여넣지 않습니다.
3. 외부 MCP 또는 동적 스킬 레지스트리를 연결할 때 해당 서비스의 이용 약관·비용·데이터 반출 범위를 검토하고, HTTPS 호스트를 `SKILL_REGISTRY_ALLOWLIST`에 명시합니다. 신뢰되지 않은 MCP의 도구 승인을 해제하지 않습니다.
4. GitHub 저장소 Settings에서 Actions 실행을 허용하고, `main` 브랜치에 `verify`와 `secrets` 체크를 요구하는 branch protection을 선택적으로 설정합니다. 원격 저장소 연결과 코드 푸시는 이미 완료되어 별도 주소나 토큰 입력이 필요하지 않습니다.
5. 제출 전 최종 원고의 인용 원문, 연구윤리, 개인정보 처리, IRB 필요 여부, 저널 저자 지침을 연구자와 지도교수가 직접 검토합니다. 생성 결과는 학술적·법적 책임을 대신하지 않습니다.

로컬 Supabase의 URL과 API key는 `npm run supabase:env`가 자동으로 갱신하므로 직접 찾거나 이 파일에 기록하지 않습니다.
