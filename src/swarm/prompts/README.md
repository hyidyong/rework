# Agent prompt registry

The executable prompt registry lives in `src/swarm/agents.ts` so prompts remain bundled and type-linked to their Zod output contracts. The files in this directory are reviewable persona cards; remote skills may only add verified prompt text and never executable code.
